# Example environment variables for the API server.
# Copy this file to .env and adjust the values for your local or staging setup.

NODE_ENV=development
PORT=8788

# Comma separated list of allowed origins for CORS responses.
ALLOWED_ORIGINS=https://thesara.space,http://localhost:3000
WEB_BASE=http://localhost:3000
PUBLIC_BASE=http://127.0.0.1:8788

# JWT signing configuration used by auth middleware and room tokens.
JWT_SECRET=change-me
JWT_ISSUER=thesara-api
# Optional audience value; leave empty when not validating.
JWT_AUDIENCE=

# Storage configuration (switch STORAGE_BACKEND to gcs for Google Cloud Storage).
STORAGE_BACKEND=local
THESARA_STORAGE_ROOT=../../storage/kv
BUNDLE_STORAGE_PATH=../../storage/bundles
PREVIEW_STORAGE_PATH=../../storage/previews
KV_STORAGE_PATH=../../storage/kv
TMP_PATH=../../storage/tmp

# Firebase / Google Cloud credentials (required when STORAGE_BACKEND=gcs).
FIREBASE_PROJECT_ID=your-firebase-project
FIREBASE_STORAGE_BUCKET=your-firebase-project.appspot.com
GOOGLE_APPLICATION_CREDENTIALS=../keys/createx-service-account.json

# Stripe configuration (populate with live keys in production).
STRIPE_SECRET_KEY=sk_test_change_me
STRIPE_WEBHOOK_SECRET=whsec_change_me

# Toggle for exposing /api/jwt in non-test environments (keep disabled in production).
DEV_ENABLE_LOCAL_JWT=0
