# THESARA - Production Runbook

Fast path checklist
-------------------
- [ ] Git repo at `/srv/thesara/app` up to date (`git pull --ff-only`).
- [ ] Builds ready: `pnpm -C apps/api build` and `pnpm -C apps/web build`.
- [ ] nginx config matches *Standard nginx config* below (reload after edits).
- [ ] Next legacy redirect handlers deployed (`apps/web/app/api/listing/route.ts`, `/app/play/(legacy)/route.ts`).
- [ ] UI uses `playHref` helper and `prefetch={false}` on play `Link`.
- [ ] PM2 points API to `apps/api/dist/server.cjs`; both apps use `.env.production`.
- [ ] Smoke tests (section *Smoke validation*) all green.
- [ ] Admin review endpoints tested with the cURL snippets.
- [ ] Player assets load from `/builds/<id>/` via nginx proxy to port 8788.
- [ ] Document any deviations here before closing the incident.

Topology
--------
- **Web (Next.js)**: `apps/web`, listens on `127.0.0.1:3000`, PM2 process `thesara-web`.
- **API (Fastify)**: `apps/api`, listens on `127.0.0.1:8788`, PM2 process `thesara-api`. Serves runtime HTML, `/builds/`, `/uploads/`, `/previews/`.
- **nginx**: terminates TLS for `thesara.space` and proxies to the services above. `/api/jwt` stays on the web app; all other `/api/` routes go to the API host.
- **Storage**: `/srv/thesara/storage` (subdirs `bundles`, `uploads`, `previews`) mounted to the API process.
- **Configs**: `.env.production` per app, PM2 `ecosystem.config.cjs`, nginx vhost file under `/etc/nginx/sites-available/thesara.space`.

Standard nginx config
---------------------

````nginx
# =========================
# HTTPS vhost: thesara.space (WEB)
# =========================
server {
  listen 443 ssl http2;
  server_name thesara.space;

  # TLS
  ssl_certificate     /etc/letsencrypt/live/thesara.space/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/thesara.space/privkey.pem;
  include             /etc/letsencrypt/options-ssl-nginx.conf;

  # 1) Next.js route handler - stays on WEB (not API)
  location = /api/jwt {
    proxy_pass http://127.0.0.1:3000/api/jwt;
    proxy_http_version 1.1;
    proxy_set_header Host              thesara.space;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
  }

  # 2) All other /api/ -> API vhost (or 127.0.0.1:8788 if co-hosted)
  location ^~ /api/ {
    proxy_pass https://api.thesara.space; # or http://127.0.0.1:8788;
    proxy_http_version 1.1;
    proxy_set_header Host              api.thesara.space; # preserve vhost
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
    proxy_connect_timeout 15s;
    client_max_body_size 50m;
  }

  # 3) Runtime mini-apps & bundles - API 8788
  location ^~ /play/ {
    proxy_pass http://127.0.0.1:8788;
    proxy_http_version 1.1; proxy_set_header Connection "";
    proxy_set_header Host              $host;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_redirect off;
  }

  location ^~ /builds/ {
    proxy_pass http://127.0.0.1:8788;
    proxy_http_version 1.1; proxy_set_header Connection "";
    proxy_set_header Host              $host;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_redirect off;
    # If upstream does not set it, uncomment the next line:
    # add_header Cross-Origin-Resource-Policy "cross-origin" always;
    expires 7d; access_log off;
  }

  location ^~ /uploads/ {
    proxy_pass http://127.0.0.1:8788;
    proxy_http_version 1.1; proxy_set_header Connection "";
    proxy_set_header Host              $host;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_redirect off;
    expires 7d; access_log off;
  }

  location ^~ /previews/ {
    proxy_pass http://127.0.0.1:8788;
    proxy_http_version 1.1; proxy_set_header Connection "";
    proxy_set_header Host              $host;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_redirect off;
    expires 7d; access_log off;
  }

  # 4) Next.js frontend (everything else)
  location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host       $host;
    proxy_cache_bypass          $http_upgrade;
  }

  # Optional: longer cache for static via Next
  location ~* \.(?:css|js|png|jpg|jpeg|gif|svg|ico|webp|woff2?|ttf)$ {
    proxy_pass http://127.0.0.1:3000;
    expires 30d;
    access_log off;
  }
}

# HTTP -> HTTPS redirect
server {
  listen 80;
  server_name thesara.space;
  location ^~ /.well-known/acme-challenge/ { root /var/www/html; }
  return 301 https://$host$request_uri;
}
````

Keep `/play`, `/builds`, `/uploads`, and `/previews` on the web origin to avoid CORP violations. `/api/jwt` stays on web because it is implemented as a Next route handler.

Next.js routing and helpers
---------------------------
- `/api/listing/<slug>` is canonical. The legacy query entry point is `apps/web/app/api/listing/route.ts` and sends a 307 redirect.
- `/play/<appId>/` is canonical. `apps/web/app/play/(legacy)/route.ts` redirects query-only links with a 308.
- `apps/web/app/play/[appId]/page.tsx` renders the canonical play page via `ClientPlayPage`.
- `apps/web/lib/urls.ts` exposes `playHref(appId, params?)`. Always generate Play CTAs with it and set `prefetch={false}` on Next `Link` to avoid unintended preloads.
- UI should gate listing details by `item.status === "published"` for public visitors; owners/admins may see unpublished notices.

Deploy workflow (detailed)
--------------------------
1. `git pull --ff-only` inside `/srv/thesara/app`.
2. `pnpm install --frozen-lockfile` (once per deploy to align lockfile).
3. `pnpm -C apps/api build` (outputs `apps/api/dist/server.cjs`).
4. `pnpm -C apps/web build` (Next production bundle).
5. `pm2 restart thesara-api --update-env` and `pm2 restart thesara-web --update-env`.
6. `sudo nginx -t && sudo systemctl reload nginx` if config changed.
7. Run *Smoke validation* below.
8. Record outcomes and any incidents back in this runbook.

PM2 and build gotchas
---------------------
- `Cannot find module .../apps/api/dist/server.cjs`: rebuild (`pnpm -C apps/api build`) and confirm PM2 `script` plus working directory.
- Play returns 404 or blank: confirm both redirect handlers are deployed, nginx proxies `/play/` and `/builds/` to 8788, and CTAs call `playHref`.
- Listing loads HTML 404: client must call `/api/listing/<slug>`, not the legacy query version, and the API must have the listing `status` set to `published`.
- JWT signing fails: `/api/jwt` must stay on port 3000; verify `JWT_SECRET` matches in both `.env.production` files.
- Worker queues missing: ensure `CREATEX_WORKER_ENABLED=true` in API env and that the PM2 env is refreshed with `--update-env`.

Admin review API snippets
-------------------------

````bash
# Fresh admin token (web route handler)
TOK=$(curl -s http://127.0.0.1:3000/api/jwt -X POST \
  -H 'content-type: application/json' -d '{"userId":"debug","role":"admin"}' | jq -r .token)

# Approve
ID=<BUILD_ID>
curl -i -X POST https://api.thesara.space/review/approve/$ID \
  -H "Authorization: Bearer $TOK"

# Reject with reason
curl -i -X POST https://api.thesara.space/review/reject/$ID \
  -H "Authorization: Bearer $TOK" -H 'content-type: application/json' \
  -d '{"reason":"manual test"}'

# Filter review queue
curl -s 'https://api.thesara.space/review/builds?status=approved' \
  -H "Authorization: Bearer $TOK" | jq '.items | length'
````

Smoke validation
----------------

````bash
# 1) Listing detail (published -> 200 JSON)
curl -s https://thesara.space/api/listing/image-compressor | jq '.item | {slug,status,title}'

# 2) Legacy listing endpoint redirects (307)
curl -I -s 'https://thesara.space/api/listing?slug=image-compressor' | grep -i '307\|location'

# 3) Play legacy redirect (308)
curl -I -s 'https://thesara.space/play?appId=4' | grep -i '308\|location'

# 4) Canonical play (200 HTML)
curl -I -s 'https://thesara.space/play/4/' | grep -i '200\|content-type'

# 5) Bundle HTML + first JS asset (200)
BID=$(curl -s https://thesara.space/api/listing/image-compressor | jq -r '.item.buildId')
curl -I -s "https://thesara.space/builds/$BID/bundle/" | grep -i '200\|content-type'
curl -I -s "https://thesara.space/builds/$BID/bundle/inline-*.js" | head -n1
````

Troubleshooting scenarios
-------------------------
- **Player page infinite loads**: legacy `/play?appId=` redirect missing. Deploy `apps/web/app/play/(legacy)/route.ts`, clear Next build, reload nginx to proxy `/play/`.
- **Assets blocked by CORP**: nginx missing `/builds/` proxy or upstream stripped `Cross-Origin-Resource-Policy`. Add the block above; optionally uncomment the header line.
- **Listing renders "owner only" banner to public**: API returned `status !== "published"`. Check review state via admin endpoints and publish build if ready.
- **Review queue empty**: confirm worker processed the build, check API logs for moderation errors, then use the filter snippet to inspect other states.
- **JWT requests hitting API**: nginx misroutes `/api/jwt`; fix location block, run `sudo nginx -t`, then reload.
- **Build uploads fail with large body**: ensure nginx `/api/` block has `client_max_body_size 50m` and API env points to correct storage paths.

Reference commands
------------------
```
pm2 status
pm2 logs thesara-api --lines 100
pm2 logs thesara-web --lines 100
curl -sSI http://127.0.0.1:8788/health
curl -sSI https://api.thesara.space/api/health
```

Always update this runbook when routing, proxying, or deploy flow changes. No change is "done" until the checklist at the top passes end to end.

