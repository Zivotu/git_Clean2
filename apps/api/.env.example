# This file contains environment variables for the API server.
# Copy this file to .env and fill in the values.

# -- Firebase Admin SDK --
# Used for authentication and other Firebase services.
# You can get this from your Firebase project settings.
# It's a JSON string, so it should be enclosed in single quotes.
# FIREBASE_SERVICE_ACCOUNT_JSON=''

# -- JWT Settings --
# Secret key for signing JWTs. Used as a fallback and for the dev /api/jwt endpoint.
# Generate a strong, random string for this.
JWT_SECRET=your-super-secret-key-change-me

# Issuer for JWTs
# JWT_ISSUER=thesara-api

# Audience for JWTs
# JWT_AUDIENCE=thesara-app

# -- Development-only --
# Set to "1" to enable the /api/jwt endpoint even in production.
# WARNING: This is a security risk. Only use for specific testing scenarios.
DEV_ENABLE_LOCAL_JWT=1

# --- Storage backend ---
# LOCAL (datotečni sustav) ili GCS (Google Cloud Storage). Za lokalni dev koristi LOCAL.
STORAGE_BACKEND=LOCAL

# Obavezno za LOCAL backend (apsolutna putanja). Npr. na Windowsu:
# KV_STORAGE_PATH=C:\thesara_RollBack\storage\kv
KV_STORAGE_PATH=../../storage/kv

# -- Other --
# Port for the API server
# PORT=8788

# Base URL for the web app (used for CORS and other links)
# WEB_BASE=http://localhost:3000

# -- Bundler Settings --
# BUNDLE_MODE=self # 'self' to bundle, 'cdn' to use import maps
# BUNDLE_CACHE_DIR=./.cache # Cache directory for remote dependencies
